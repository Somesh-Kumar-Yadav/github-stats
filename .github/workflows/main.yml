# ============================================================
# THIS FILE GOES IN YOUR github-stats REPO, NOT YOUR PROFILE REPO
# Repo: github.com/Somesh-Kumar-Yadav/github-stats
# ============================================================

name: Generate Stats Images

on:
  push:
    branches: [ master ]
  schedule:
    - cron: "0 0 * * *"   # daily at midnight UTC
  workflow_dispatch:        # manual trigger

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Install dependencies
        run: pip install requests aiohttp python-dateutil

      - name: Generate images
        run: python generate_images.py
        env:
          ACCESS_TOKEN: ${{ secrets.ACCESS_TOKEN }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          EXCLUDED:     ${{ secrets.EXCLUDED }}
          EXCLUDED_LANGS: ${{ secrets.EXCLUDED_LANGS }}

      - name: Commit and push generated SVGs
        run: |
          git config --global user.name  "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add generated/
          git diff --cached --quiet || git commit -m "Update generated stats images [skip ci]"
          git push
